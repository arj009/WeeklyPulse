# Fixtures (`data/fixtures/`)

Sanitized or synthetic App Store / Play Store exports for tests (Phase 2).

- Safe to commit — no real user PII.
- Used by ingestion unit tests and local dev without production exports.
